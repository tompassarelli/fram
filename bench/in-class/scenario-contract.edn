{:contract-version 1
 :corpus-size-unit :live-triples
 :default-corpus-sizes [3000 30000]
 :default-runs 2
 :workload
 {:warmup-writes 30
  :warmup-reads 10
  :measured-writes 1200
  :mixed-cycles 40
  :mixed-reads-per-cycle 3}
 :scenarios
 [{:id :boot-to-serving
   :measure :boot-to-serving-ms
   :definition "Open the durable corpus and reach the adapter-ready probe."}
  {:id :cold-start-query
   :measure :cold-start-query-ms
   :definition "First two-literal kind+title join after boot, before warmup."}
  {:id :sustained-write-under-read
   :measure :write-under-read-ops-s
   :definition "One sole writer commits distinct triples while concurrent readers repeat the same join."}
  {:id :mixed-read-write
   :measure :mixed-ops-s
   :definition "Fixed one-write/three-read cycles after warmup."}]
 :required-result-keys
 [:adapter :run :corpus-triples :boot-to-serving-ms
  :cold-start-query-ms :cold-query-rows :write-under-read-ops-s
  :concurrent-read-ops :mixed-ops-s :mixed-read-p50-ms :errors]
 :golden
 {:max-latency-regression-ratio 1.50
  :min-throughput-retention-ratio 0.67
  :metrics
  {:boot-to-serving-ms :lower-is-better
   :cold-start-query-ms :lower-is-better
   :write-under-read-ops-s :higher-is-better
   :mixed-ops-s :higher-is-better}}

 :index-architecture
 {:contract-version 1
  :corpus-size-unit :live-triples
  :default-corpus-sizes [3000 30000 300000]
  :rotation-corpus-size 350701
  :default-runs 2
  :engines ["store-id-hash" "mmap-rotations"]
  :required-result-keys
  [:receipt-kind :contract-version :scenario :decision-section :query-class
   :engine :run :corpus-triples :prepare-ms :query-ms :result-count
   :expected-count :errors :rss-before-kb :rss-retained-kb :heap-retained-bytes
   :storage-bytes :started-utc :revision :kernel :nproc :cpu-model
   :mem-total-kb :load-start :load-end :java-version :babashka-version]
  :scenarios
  [{:id :coordinator-aggregate-scan
    :query-class :coordinator-aggregate-scan
    :corpus-sizes :default
    :decision-section "Workload derivation / Coordinator aggregate/projection scans"
    :definition "Bound-predicate title projection; returning K rows remains O(K)."}
   {:id :staffing-projection
    :query-class :staffing-projection
    :corpus-sizes :default
    :decision-section "Workload derivation / Staffing"
    :definition "Select the fixed 32 kind=agent subjects through POS, then project each subject through SPO."}
   {:id :point-lookup
    :query-class :point-lookup
    :corpus-sizes :default
    :decision-section "Workload derivation / Point lookup"
    :definition "Exact subject+predicate lookup for @corpus-0 title through SPO; result cardinality is one."}
   {:id :compound-datalog-join
    :query-class :compound-datalog-join
    :corpus-sizes :default
    :decision-section "Workload derivation / Datalog joins"
    :definition "Bind title-0 through POS, then re-probe the substituted subject against kind=agent and owner=@owner-0; result cardinality is one."}
   {:id :rotation-outage-350701
    :query-class :compound-datalog-join
    :corpus-sizes [350701]
    :decision-section "Benchmark interpretation"
    :definition "Reproduce the 350,701-live-fact two-rule lead->title shape with exactly 1,623 rows."
    :receipt-fields
    {:historical-before-ms 5004
     :historical-after-ms 241
     :historical-after-rows 1623
     :historical-source "~/code/fram/bench/index-rotations/README.md:46-79"
     :historical-timing-claim "provenance-only; current timings are newly observed"}}]}}
